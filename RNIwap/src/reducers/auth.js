import { SET_USER, RESET_USER } from '../actions';

export default authReducer = (state, action) => {
  switch (action.type) {
    case SET_USER:
    case TOGGLE_TODO:
    default:
      return state;
  };
};
